生成后放到Plugins/Android/bin
依赖的jar放到Plugins/Android/libs
工程的资源放到Plugins/Android/res
依赖的资源放到Plugins/Android/assets
启动画面splash可以放在asserts/bin/Data中
图标要分别放到各种不同精度的文件夹中，Unity会补充生成默认精度的
有些插件需要assets/res中的内容，sdk未给出，需要反编译sdk demo，从中获取
向插件目录复制文件，先SDK的，再工程的
不要勾选isLibrary，否则不会自动创建依赖库的R类，编译的jar也不包含任何R类，必须使用Export导出jar（勾选gen）
UnityPlayerNativeActivity中的mUnityPlayer是protected，可自由控制视图显示方式或切换
UnityPlayerActivity中的mUnityPlayer是private
android-support-v4在有些插件中导致编译期间错误（Unknown Error dx）
Bundle中取值的时候必须使用存储时的类型，不会自动转换
BwHandler不能在Unity客户端调用(GcInit)中创建
发布时不能使用debug模式，否则后台无记录
插件中libs/armeabi/*.so文件会被svn全局忽略，应设置svn
插件中可能未使用@JavascriptInterface开放java接口而导致Anroid4.2以上运行时出现异常：E/Web
Console: Uncaught TypeError: Object [object Object] has no method，目标版本调小些或要求sdk方修改代码
mainfest中配置的各activity都可以设置landscape/portrait
9patch图（.9.png）不符合规范时会导致打包失败，有些渠道包内的资源不正确
游戏名字应该使用app_name，否则Unity中对名称的修改就无效了
